import React from "react";
import Segment from '../segment'

const Body = (<div><h1>React</h1></div>)

const Skill = () => {
  return <Segment icon="align-left" name="Skill" component={Body} />;
};

export default Skill;
